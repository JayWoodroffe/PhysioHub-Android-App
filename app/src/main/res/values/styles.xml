<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.LoginAndSignUp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowAnimationStyle">@null</item>       <item name="android:textColor">@android:color/black</item>
    </style>

    <!-- Custom EditText style -->
    <style name="CustomEditText" parent="Widget.AppCompat.EditText">
        <item name="android:padding">12dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="hintTextColor">@color/mainBlue</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="LoginEditTextOuter" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">5dp</item>
        <item name="android:layout_marginBottom">5dp</item>
        <item name="android:textSize">16sp</item>
        <item name="boxStrokeColor">@color/mainBlue</item>
        <item name="hintTextColor">@color/mainBlue</item>
        <item name="android:textColor">@color/mainBlue</item>
        <item name="android:textCursorDrawable">@color/black</item>

    </style>

    <style name="LoginEditTextInner" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/mainBlue</item>
        <item name="android:maxLines">1</item>
        <item name="android:textSize">14sp</item>
        <item name="android:singleLine">true</item>
    </style>


    <style name="DatePickerTheme" parent="Theme.AppCompat.Light.Dialog">
        <!-- Other style attributes -->
        <item name="colorAccent">@color/mainBlue</item>
        <!-- Add the following attributes to include OK and Cancel buttons -->
        <item name="android:buttonBarPositiveButtonStyle">@style/MyPositiveButtonStyle</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/MyNegativeButtonStyle</item>
    </style>

    <style name="TimePickerTheme" parent="Theme.AppCompat.Light.Dialog">
        <!-- Other style attributes -->
        <item name="colorAccent">@color/mainBlue</item>
        <!-- Add the following attributes to include OK and Cancel buttons -->
        <item name="android:buttonBarPositiveButtonStyle">@style/MyPositiveButtonStyle</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/MyNegativeButtonStyle</item>
    </style>

    <style name="MyPositiveButtonStyle" parent="@android:style/Widget.Button">
        <!-- Customize positive button style if needed -->
    </style>

    <style name="MyNegativeButtonStyle" parent="@android:style/Widget.Button">
        <!-- Customize negative button style if needed -->
    </style>


</resources>